function shout (string) {
    //to do 
    return string.toUpperCase(); 
}

function whisper (string) {
    //to do 
    return string.toLowerCase(); 
}


function logShout(){
    
    const spy = "hello"

    console.log (logShout(spy).toUpperCase());
}